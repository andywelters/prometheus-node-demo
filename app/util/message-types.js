'use strict';

const prefix = 'APP_NAME_THAT_IS_UNIQUE:';
module.exports = messageTypes = {
  GET_CLUSTER_METRICS: prefix+'get-cluster-metrics',
  POST_CLUSTER_METRICS: prefix+'post-cluster-metrics',
};